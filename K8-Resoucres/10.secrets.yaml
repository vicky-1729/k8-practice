# Secret for sensitive values (base64-encoded data).
# IMP: base64 is encoding, not encryption; protect secret access with RBAC.
apiVersion: v1
kind: Secret
metadata:
  name: app-secret
type: Opaque
data:
  USERNAME: "YWRtaW4="
  PASSWORD: "dGVzdA=="

