# Pod that imports all keys from ConfigMap as environment variables.
apiVersion: v1
kind: Pod
metadata:
  name: config-consumer-pod
spec:
  containers:
    - name: nginx
      image: nginx
      envFrom:
        - configMapRef:
            name: app-config
