# Pod that imports secret values as environment variables.
apiVersion: v1
kind: Pod
metadata:
  name: secret-consumer-pod
spec:
  containers:
    - name: nginx
      image: nginx
      envFrom:
        - secretRef:
            name: app-secret